#!/bin/sh
set -e

case "$1" in
    remove|purge)
        if command -v pam-auth-update >/dev/null 2>&1; then
            pam-auth-update --package || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
